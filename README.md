# Awais Visuals — Clean Name Fix

The previous website design, pages, services, buttons, red/green hover effects,
and layouts are kept unchanged.

Only the requested name correction was made:

- Website name is consistently `Awais`.
- The `i` has the normal single dot.
- Accented/decomposed variants were removed.
- Name-specific styling that could make the name look unusually colored was removed.
- The rest of the website remains unchanged.

Run `index.html` with VS Code Live Server.


Card-only hover update: visual/media surfaces move forward on hover while card text/captions stay fixed in place.
